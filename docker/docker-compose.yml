services:
  uniprot-link:
    build:
      context: ..
      dockerfile: docker/Dockerfile
    image: uniprot-link:latest
    # Host port defaults to 0 = Docker assigns a random free ephemeral port
    # (no conflicts with other services). Pin it by setting UNIPROT_LINK_HOST_PORT.
    # Discover the assigned port with: make docker-url
    ports:
      - target: 8000
        published: "${UNIPROT_LINK_HOST_PORT:-0}"
        protocol: tcp
    environment:
      UNIPROT_LINK_TRANSPORT: unified
      UNIPROT_LINK_HOST: 0.0.0.0
      UNIPROT_LINK_PORT: "8000"
      UNIPROT_LINK_LOG_LEVEL: INFO
      UNIPROT_LINK_LOG_FORMAT: json
      # Set a real contact mailbox per UniProt etiquette:
      UNIPROT_LINK_SPARQL__CONTACT_EMAIL: bernt.popp@charite.de
    healthcheck:
      test: ["CMD", "curl", "-fsS", "http://localhost:8000/health"]
      interval: 30s
      timeout: 3s
      retries: 3
      start_period: 20s
    restart: unless-stopped
